# Local Supabase (from repo root: npx supabase status)
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=<from npx supabase status>

# mock (default) | supabase — auth/org adapters only until later phases
VITE_DATA_ADAPTER=mock
